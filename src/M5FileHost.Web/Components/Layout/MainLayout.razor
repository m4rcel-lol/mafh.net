@inherits LayoutComponentBase

<div class="app-shell">
    <header class="topbar">
        <a class="brand" href="/"><span class="brand-mark">m5</span><span>m5rcel's awesome file hoster</span></a>
        <nav class="top-actions">
            <AuthorizeView>
                <Authorized><a class="avatar" href="/settings" aria-label="Account">@context.User.Identity?.Name?.Substring(0, 1).ToUpperInvariant()</a></Authorized>
                <NotAuthorized><a class="button tonal compact" href="/login">Sign in</a></NotAuthorized>
            </AuthorizeView>
        </nav>
    </header>
    <aside class="nav-rail" aria-label="Primary navigation">
        <NavLink href="/" Match="NavLinkMatch.All"><span>⌂</span><small>Home</small></NavLink>
        <NavLink href="/uploads"><span>◫</span><small>Browse</small></NavLink>
        <AuthorizeView><Authorized>
            <NavLink href="/upload" class="rail-fab"><span>＋</span><small>Upload</small></NavLink>
            <NavLink href="/dashboard"><span>◉</span><small>Dashboard</small></NavLink>
            <AuthorizeView Policy="Moderator"><Authorized Context="adminState"><NavLink href="/admin"><span>⚙</span><small>Admin</small></NavLink></Authorized></AuthorizeView>
        </Authorized></AuthorizeView>
    </aside>
    <main class="content">@Body</main>
    <nav class="bottom-nav" aria-label="Mobile navigation">
        <NavLink href="/" Match="NavLinkMatch.All"><span>⌂</span><small>Home</small></NavLink>
        <NavLink href="/uploads"><span>◫</span><small>Browse</small></NavLink>
        <AuthorizeView><Authorized><NavLink href="/upload"><span>＋</span><small>Upload</small></NavLink><NavLink href="/dashboard"><span>◉</span><small>You</small></NavLink></Authorized></AuthorizeView>
    </nav>
</div>
